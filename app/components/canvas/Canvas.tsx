'use client';

export interface CanvasProps {}

export default function Canvas(_props: CanvasProps) {
  return (
    <section aria-label="canvas" className="canvas-stub">
      <p className="serif-italic">(Canvas not implemented — Phase B unit 8)</p>
    </section>
  );
}

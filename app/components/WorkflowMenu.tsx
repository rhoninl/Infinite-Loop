'use client';

export default function WorkflowMenu() {
  return (
    <div aria-label="workflow menu" className="workflow-menu-stub">
      <span className="serif-italic">(menu — Phase B unit 13)</span>
    </div>
  );
}

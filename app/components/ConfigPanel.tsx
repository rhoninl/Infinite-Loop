'use client';

export default function ConfigPanel() {
  return (
    <aside aria-label="config panel" className="config-stub">
      <p className="serif-italic">(Config panel not implemented — Phase B unit 11)</p>
    </aside>
  );
}

node_modules
.next
.next/
dist
out
.DS_Store
*.log
.env
.env.local
.env.*.local
coverage
.vscode
.idea
*.tsbuildinfo

# User-generated run history (per-workflow JSON records). Anchored to the
# repo root so it doesn't accidentally swallow `app/api/runs/`.
/runs/
